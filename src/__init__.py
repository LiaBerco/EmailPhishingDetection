"""Student A — Content / NLP phishing detector: reusable helpers.

The heavy modelling lives in the notebook (so it can be read top to bottom);
these modules hold the utility pieces that the notebook imports: configuration,
data harmonisation, the normalisation defence, the obfuscation attacks, and the
small statistics helpers (bootstrap confidence intervals + McNemar's test).
"""
